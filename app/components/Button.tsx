import React from "react";
import PropTypes from "prop-types";

Button.propTypes = {
  // todo: make dark and light mode button
};

function Button(props) {
  return (
    <div></div>
  );
}

export default Button;
